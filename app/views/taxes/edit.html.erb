<div class="container">
  <div class="col-md-3">
<%= render 'navbar' %>
  </div>
  <div class="col-md-9">
<h1>Taxes</h1>

<%= render 'form' %><br />

<%= link_to "Back", buyer_tax_path(@buyer, @tax), class: "btn btn-primary" %> |
<%= link_to "Next", new_buyer_lien_path(@buyer, @lien), class: "btn btn-primary" %>
  </div>
